import { BasicHiglightDirective } from './basic-higlight.directive';

describe('BasicHiglightDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicHiglightDirective();
    expect(directive).toBeTruthy();
  });
});
